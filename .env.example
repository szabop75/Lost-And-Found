# Docker Compose környezeti változók helyi fejlesztéshez
# Másold ezt a fájlt .env vagy .env.development néven és töltsd ki a megfelelő értékekkel

# PostgreSQL
POSTGRES_DB=lostandfound
POSTGRES_USER=your-db-user
POSTGRES_PASSWORD=your-db-password
POSTGRES_PORT=5432

# ASP.NET Core
ASPNETCORE_ENVIRONMENT=Development

# JWT beállítások (éles környezetben erős, titokban tartott kulcsot használj)
JWT__SECRET=dev_super_secret_key_change_me
JWT_ISSUER=LostAndFoundIssuer
JWT_AUDIENCE=LostAndFoundAudience
