# Frontend production environment sample
# Copy this file to .env.production on the frontend server/build env
# and adjust values as needed.

# The backend API public URL
VITE_API_BASE_URL=http://192.168.1.2:11500

# Optional: if using a different base path for the app when served behind a reverse proxy
# VITE_BASE_PATH=/
